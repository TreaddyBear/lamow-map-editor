import { test, expect } from "@playwright/test";
import { previewState } from "./preview-state";
import { readFile } from "node:fs/promises";

test("five simultaneous views share an engine and calibrated coverage survives export and reload", async ({ page }) => {
  const errors: string[] = []; page.on("pageerror", error => errors.push(error.message));
  await page.goto("/assets");
  const canvas = page.getByTestId("vegetation-preview-board");
  await expect(canvas).toHaveAttribute("data-update-ms", /\d/);
  const runtime = await previewState(page);
  expect(runtime.engineCount).toBe(1); expect(runtime.cameras).toHaveLength(5);
  for (const id of ["plant", "half", "full", "lod-half", "lod-full"]) await expect(page.getByTestId(`preview-${id}`)).toBeVisible();
  await expect(page.getByTestId("preview-full")).toHaveAttribute("data-plant-count", "400");
  await expect(page.getByTestId("preview-half")).toHaveAttribute("data-plant-count", "200");
  const input = page.locator("label").filter({ hasText: "100% coverage · plants/m²" }).locator("input");
  await input.fill("40"); await input.blur();
  await expect(page.getByTestId("preview-full")).toHaveAttribute("data-plant-count", "640");
  await expect(page.getByTestId("preview-half")).toHaveAttribute("data-plant-count", "320");
  const downloadPromise = page.waitForEvent("download");
  await page.getByRole("button", { name: "Export", exact: true }).click();
  const download = await downloadPromise;
  expect(JSON.parse(await readFile((await download.path())!, "utf8")).species.coverage.plantsPerSquareMeter).toBe(40);
  await page.reload(); await expect(input).toHaveValue("40");
  await expect(page.getByTestId("preview-full")).toHaveAttribute("data-plant-count", "640");
  await page.getByRole("button", { name: "Plant view options", exact: true }).click();
  await page.getByRole("menuitem", { name: "Top", exact: true }).click();
  await expect(page.getByRole("alert")).toHaveCount(0);
  await page.screenshot({ path: ".tmp/five-pane-workspace.png" });
  await page.getByRole("button", { name: "Grow stem", exact: true }).click();
  await page.getByTestId("variation-distance").locator("input").first().fill("0.4");
  await page.getByTestId("variation-distance").locator("input").first().blur();
  await page.getByTestId("variation-arc-degrees").locator("input").first().fill("90");
  await page.getByTestId("variation-arc-degrees").locator("input").first().blur();
  await page.getByRole("button", { name: "Plant view options", exact: true }).click();
  await page.getByRole("menuitem", { name: "Perspective", exact: true }).click();
  await page.getByRole("button", { name: "Reset Plant view", exact: true }).click();
  await page.screenshot({ path: ".tmp/curved-growth-workspace.png" });
  expect(errors).toEqual([]);
});

test("a held edit is one undo step and primitive overrides stay with their species", async ({ page }) => {
  await page.goto("/assets");
  await page.getByRole("button", { name: "Form saddle petal", exact: true }).click();
  const field = page.getByTestId("variation-length"); const input = field.locator("input").first();
  const original = await input.inputValue();
  const increase = field.getByRole("button", { name: "Increase Ideal" });
  await increase.hover(); await page.mouse.down(); await page.waitForTimeout(1250); await page.mouse.up();
  await expect(input).not.toHaveValue(original); const edited = await input.inputValue();
  await page.getByRole("button", { name: "Undo", exact: true }).click(); await expect(input).toHaveValue(original);
  await page.getByRole("button", { name: "Redo", exact: true }).click(); await expect(input).toHaveValue(edited);
  await page.getByRole("tab", { name: "Meshes", exact: true }).click();
  const x = page.getByTestId("obj-vertex-x").locator("input").first();
  const originalX = await x.inputValue(); await x.fill("-0.333"); await x.blur();
  await page.getByRole("combobox", { name: "Species", exact: true }).selectOption("flowerWhite");
  await page.getByRole("tab", { name: "Meshes", exact: true }).click(); await expect(x).toHaveValue(originalX);
  await page.reload();
  await page.getByRole("combobox", { name: "Species", exact: true }).selectOption("flowerBlue");
  await page.getByRole("tab", { name: "Meshes", exact: true }).click(); await expect(x).toHaveValue("-0.333");
});
